
# Temperature Converter Frontend

A tiny HTML/CSS/JS page that calls the API.

## Run
Open `index.html` directly in a browser, or serve the `frontend` folder with a static server.
Make sure the API is running on `http://localhost:3000` or update `API_BASE` in `app.js`.
